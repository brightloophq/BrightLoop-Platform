{
  "name": "@brightloop/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Supabase schema: migrations, RLS policies, transition guard trigger, generated types.",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"@brightloop/db: generated types are not linted\"",
    "test": "echo \"@brightloop/db: migrations are verified on apply (see README)\"",
    "db:reset": "supabase db reset --workdir ../..",
    "db:push": "supabase db push --workdir ../..",
    "db:diff": "supabase db diff --workdir ../..",
    "db:test": "supabase test db --workdir ../..",
    "db:verify": "bash ../../scripts/db-verify.sh",
    "gen:types": "supabase gen types typescript --linked --workdir ../.. | perl -0pe 's/(?:\\r?\\n){2,}\\z/\\n/' > ./generated/database.types.ts",
    "gen:types:local": "supabase gen types typescript --local --workdir ../.. | perl -0pe 's/(?:\\r?\\n){2,}\\z/\\n/' > ./generated/database.types.ts",
    "gen:types:check": "supabase gen types typescript --local --workdir ../.. | perl -0pe 's/(?:\\r?\\n){2,}\\z/\\n/' | diff -u ./generated/database.types.ts - && echo \"generated types are in sync\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
